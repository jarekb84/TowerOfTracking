/**
 * Shared Period Grouping Utilities
 *
 * Common period key generation, label formatting, and period limiting
 * used across analysis features (source-analysis, coverage-report).
 */

export { getPeriodKey, formatPeriodLabel } from './period-formatting'
export { limitToPeriods } from './limit-to-periods'
