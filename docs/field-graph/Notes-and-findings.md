[2026-04-19] [commit 1] npm run lint fails with 8 pre-existing errors on the branch tip (9546ef3), inherited from commit a9846a9 (V2→V3 migration / V28 parser work):

scripts/migration-data-prep/extract-v2-fields.mjs — 2× no-undef on process.
scripts/migration-data-prep/extract-v28-fields.mjs — max-statements on main, no-undef on process.
scripts/migration-data-prep/scaffold-v2-to-v3-map.mjs — 2× max-statements.
src/shared/formatting/date-issue-detection.ts:115 — complexity: 11/10 on detectDateIssue; :207 — unused _legacy binding.
These are out of scope for commit 1 but will block npm run integration-precheck for every subsequent commit on this branch until fixed. Options: (a) add an eslint-suppressions.json entry (fast), (b) a dedicated cleanup commit between commits 1 and 2, or (c) fold into commit 2's incidental cleanup. Flagging here per the "don't silently fix prior work" constraint.

[2026-04-19] [commit 1] Spec §8.4 lists BELONGS_TO_SECTION cardinality as 'one'; §15.1 updates it to 'many'. Implementation follows §15.1 (the later, explicit multi-section section). The "every field belongs to at least one section" rule will be added as a per-tag invariant in commit 6 when real BELONGS_TO_SECTION declarations land.

[2026-04-19] [commit 1] PARTICIPATES_IN_COMPOSITE_KEY was coded with targetKind: 'terminal' (any string). Spec §8.1 shows to: 'compositeKey:primary' with no matching node declaration, so a terminal is the simplest honest model until commit 14 introduces real composite-key usage. Revisit there if a CompositeKey node kind becomes useful.

[2026-04-19] [commit 2] `category:records` is declared but no matching `view:run-details:records` exists — `RECORDS_CONFIG` is defined in `section-config.ts` and included in `CATEGORIZED_FIELDS`, but `use-run-details-data.ts` never extracts or renders it today. Current catalog omits the view node to stay faithful to the present UI surface. Commit 6 (BELONGS_TO_SECTION / RENDERS_AS_IN_SECTION) or the commit that wires APPEARS_IN_VIEW for run-details should decide: either promote records to a real run-details sub-section (and add `view:run-details:records` + rendering) or drop `category:records` and surface records purely through chart views.

[2026-04-19] [commit 2] `section:enemiesDestroyedBy` is declared for completeness (it's one of the 16 V3 prefixes in `sampleData/supportedFields.json`), but run-details currently aliases `DESTROYED_BY_CONFIG = ENEMIES_HIT_BY_CONFIG` — the `enemiesDestroyedBy_*` fields have no UI surface today. Commit 3+ should flag any field whose only membership would be `section:enemiesDestroyedBy` so the graph doesn't silently promote an unused section into the UI.

[2026-04-19] [commit 3] The `APPEARS_IN_VIEW: 'at-least-one'` invariant in the commit-1 engine is incompatible with the phase-1 staging (Field nodes declared before edges). Relaxed to `'many'` with a ledger entry; commit 12 needs to decide whether `'at-least-one'` is even the right end-state. Candidates for "never appears in a view" include compound-only sources (fields that only feed a total and don't render standalone) and non-UI internal fields. Worth a ~5-minute audit when commit 12 lands instead of a blind restore.

[2026-04-19] [commit 3] Architecture spec §11.1 declares `_rank` with tags `['internal', 'tournament-only']`; commit 3 shipped only `'internal'`. The `'tournament-only'` tag has no consumer until commit 13's `CONDITIONAL_ON` edges — and `CONDITIONAL_ON _rank → enum:runType.tournament` is the spec's real end-state for that constraint, superseding the tag. Commit 13 can either add the tag alongside the edge for redundancy or drop the tag entirely; flagging so the decision is explicit.
- [2026-04-19] [commit 4] Enum-API ergonomics surfaced a real pain during the first vertical slice. Two-step `enumValueByStringValue` + `stringValueOf` leaked internal plumbing to consumers. Refactored to single-call helpers (`acceptedValuesFor`, `isAcceptedValue`, `matchAcceptedValue`, `enumValueMeta`). Pattern for future commits: every consumer-facing usage pattern should have a dedicated engine method; raw `edgesFrom`/`edgesTo`/`nodesOfKind` stays for invariant tests and engine internals only.

- [2026-04-19] [commit 4] TS enums used across hundreds of files (`RunType`, `RunTypeValue`) are kept authoritative; graph reads from the same `as const` array. Rule landed in `field-graph-for-ai.md` under "Writing consumers." Invariant test `enum-sync.invariant.test.ts` enforces equality. Same pattern applies to future enum-typed fields (`_dissonanceSubCategory` in commit 15).

- [2026-04-19] [commit 4] Switch statements / TS predicates kept for static dispatch over closed enums (`mapExplicitRunType`, `mapUrlTypeToRunType`, `run-type-selector-options`). Graph is for dynamic discovery, not a replacement for every switch. Rule landed in `field-graph-for-ai.md`.

- [2026-04-19] [commit 4] Field-ID constant duplication across consumers (`const RUN_TYPE_FIELD_ID = '_runType'` in multiple files) is a real smell. Deferred to a node-identity design exploration landing alongside this commit. For now, the string only appears in the graph catalog file that declares `_runType`'s ACCEPTS_VALUE edges.

- [2026-04-19] [commit 4] Architecture comments in consumer code are noise. Rule landed in `field-graph-for-ai.md`: consumer comments describe consumer semantics, not graph mechanics.

- [2026-04-19] [commit 4] Node-identity decision: Option B (named `*_NODE` exports in one file per node kind) + Option C polymorphic input (`string | Node`). Catalog aggregates via `Object.values(import * as ...)` — no `import.meta.glob`, no per-section files. Variable naming: `SECTION__FIELD_NAME_NODE` double-underscore. Compile-time `readonly` only; no `Object.freeze`. `enumValueMeta` extended to return color/displayName/etc. in one call (removes a two-step chain). Full shape documented in `docs/field-graph/EXPLORATION-node-identity-abc-deep-dive.md` (revised).

- [2026-04-25] [commit 4] Folded the B+C node-identity design in: every node declaration (~150 fields, 16 sections, 5 categories, 16 views, 3 schemas, 3 enum values) is now a named `*_NODE` export. The aggregator in `catalog/index.ts` uses `import * as` + `Object.values` filtered by a structural type-guard (`'id' in v && 'kind' in v`), so non-node helpers (`runTypeEnumNodeId`, `RUN_TYPE_ENUM_NODES` lookup record) coexist with node handles in the same module without polluting `CATALOG_NODES`. `RUN_TYPE_FIELD_ID` deleted from the catalog and from `field-graph/index.ts`; consumers (`run-type-display`, `run-type-filter`, `enum-values.edges`, two test files) now import `_RUN_TYPE_NODE` from `catalog/fields.nodes` and pass it through the polymorphic `FieldRef = string | Node` API. `enumValueMeta` now returns `color` alongside `displayName`, collapsing `getRunTypeColor`'s two-call chain into a single optional-chain. `run-type-detection.ts` was on the consumer-swap list but never imported `RUN_TYPE_FIELD_ID` (it operates on raw fields-record keys at the parser boundary), so it required no edit.

- [2026-04-25] [commit 4] Spurious Vite "Failed to resolve import" errors trip ~101 test files at transform time on this Windows checkout (e.g. `@/shared/locale/locale-config`, `@/shared/domain/module-data`, `@/features/data-import/csv-import/csv-helpers`). Verified pre-existing by stashing all commit-4 work and re-running: same 101 failed test files. The errors do not correspond to test-case failures — `npx vitest run` reports `1654 passed, 0 failed, 1 skipped` both before and after this commit. Looks like a Vite alias-resolution race when many test files transform in parallel; not in scope here. Worth a separate investigation pass if it persists past commit 5.

- [2026-04-25] [commit 5] `IS_INTERNAL_FIELD` shipped in commit 1 with `targetKind: 'none'` (marker edge, no `to`), so the per-field declaration is `edge(node.id, 'IS_INTERNAL_FIELD')`. Spec section 11.1 still shows the older draft pointing at an `'internal:app-metadata'` `InternalFieldSet` node — that node-kind was never added to the engine's `NodeKind` union, so the marker form is the locked design. Future commits should use `edgesByType.get('IS_INTERNAL_FIELD').map(e => e.from)` (mirrored as `graph.internalFields()`) rather than walking edges to a sentinel node. Spec section 11.1 could be tightened to reflect the as-shipped form, but that's a docs follow-up, not a code one.

- [2026-04-25] [commit 5] CSV column ordering for internal fields is encoded as **declaration order in `catalog/internal-fields.edges.ts`** (Date → Time → Notes → Run Type → Rank). `graph.internalFields()` returns ids in the order their `IS_INTERNAL_FIELD` edges appear, and the exporter's column-rank map is built from that array. Reordering the array in the catalog file reorders CSV columns. Worth a one-line code comment at the top of `internal-fields.edges.ts` (already present) but flagging here too: future internal-field additions go at the end of the spec list unless deliberately changing on-disk format.

- [2026-04-25] [commit 5] `INTERNAL_FIELD_MAPPINGS` and `INTERNAL_FIELD_ORDER` deleted from `internal-field-config.ts` per the epic scope. `INTERNAL_FIELD_NAMES`, `LEGACY_FIELD_MIGRATIONS`, `isInternalField`, `isLegacyField`, `getMigratedFieldName`, `getInternalFieldNamesSet` retained — they belong to other commits. Specifically: `LEGACY_FIELD_MIGRATIONS` becomes `RENAMED_FROM` edges in commit 10, and `INTERNAL_FIELD_NAMES` is the TS-side source-of-truth string constant (parallel to `RUN_TYPE_VALUES`) — used by ~9 consumer files to address fields by name. Don't touch it without a plan for those consumers; commit 10's RENAMED_FROM cutover is the natural place to revisit whether the constant earns its keep alongside the `*_NODE` exports.

- [2026-04-25] [commit 5] Migration-era ledger entry says the bulk-export E2E (`e2e/features/data-import/bulk-export.spec.ts`) is "unblocked by: commit 5 (HAS_CSV_HEADER) **and** fixture regeneration." HAS_CSV_HEADER is now declared, but the fixture (`expected-bulk-export.csv`) is still V2-era and the export emits `v3_`-prefixed canonical keys. Did NOT re-enable the test in this commit since fixture regeneration is its own scope decision (which V3 keys / what header shape is the new canonical output). Suggest folding the unskip into a later commit alongside an explicit fixture refresh — likely commit 6 (BELONGS_TO_SECTION) or commit 7 (sources) when the export's canonical column set settles.

- [2026-04-25] [commit 5] Engine helpers added: `internalFields()`, `isInternalField(fieldRef)`, `csvHeaderOf(fieldRef)`. All polymorphic on `FieldRef` per the locked convention. The CSV exporter's `getAllFieldKeys` consumes `internalFields()` and reuses the resulting rank Map for the membership check (single source of truth for both ordering and "is this internal?"). `isInternalField` on the graph parallels the existing `isInternalField` in `internal-field-config.ts` — the latter checks against the TS-side enum (`INTERNAL_FIELD_NAMES`) and stays as a string-narrowing predicate at the parser/migration boundary; the former checks for the marker edge for consumers that already have a graph in hand. Pattern-drift + architecture review pushed for an `enum-sync`-style invariant; landed `internal-fields-sync.invariant.test.ts` next to the run-type one. Drains alongside `INTERNAL_FIELD_NAMES` if commit 10's RENAMED_FROM cutover decides to delete the TS-side constant.

- [2026-04-25] [commit 5] Post-review user feedback drove three further changes:
  1. **`internal-fields.edges.ts` simplified.** Dropped the `INTERNAL_FIELD_SPECS` table + `flatMap` indirection. Replaced with a tiny `internalFieldEdges(node, csvHeader)` helper called once per field — reads top-to-bottom as five per-field declarations, each emitting two edges. Same output, no spec/flatMap roundtrip. The original spec/flatMap pattern was copied from `enum-values.edges.ts` (commit 4), but that file's spec table is doing real work — it iterates the TS-source-of-truth tuple `RUN_TYPE_VALUES`. Internal fields don't have an equivalent ordered tuple, so the spec table was indirection without payoff.
  2. **Dead exports drained.** `isInternalField(string)` in `internal-field-config.ts` had only one production caller (csv-exporter), and the post-review fix to use the rank Map made it unreferenced. `getInternalFieldNamesSet()` was already dead before commit 5 (only its own test imported it). Both deleted along with their describe blocks. `internal-field-config.ts` is now down to `INTERNAL_FIELD_NAMES`, `InternalFieldName`, `LEGACY_FIELD_MIGRATIONS`, `isLegacyField`, `getMigratedFieldName` — and the latter three are scoped into commit 10's cutover (epic updated).
  3. **Commit 10 scope expanded.** `LEGACY_FIELD_MIGRATIONS` becomes `RENAMED_FROM` edges with `atSchema: SCHEMA_V2_NODE.id`. `isLegacyField` / `getMigratedFieldName` get deleted; their callers (csv-parser, csv-field-mapping, data-parser) move to `graph.resolveFieldByAnyKey()`. End state: `internal-field-config.ts` is just `INTERNAL_FIELD_NAMES` + its type (≤30 lines).

- [2026-04-25] [commit 5] **Open architectural question — `'internal'` tag vs IS_INTERNAL_FIELD edge duplication.** `_DATE_NODE` etc. were declared in commit 3 with `tags: ['internal']`. Commit 5 added `IS_INTERNAL_FIELD` edges. The tag's only consumers today are two assertions in `fields.nodes.test.ts` ("internal fields carry the tag" / "non-internal fields do not"), which could equivalently call `graph.isInternalField(node)`. Architecture spec §11.1 originally justified keeping both ("the tag is referenced in section 10's invariants — `if (field.tags?.includes('internal')) continue`") but those invariant tests haven't shipped, so the tag is currently load-bearing for nothing the edge doesn't already cover. Decision NOT made in commit 5 (out of scope — would touch commit 3's declarations). Folded into the commit 5b API-revisit ADR or, alternatively, into commit 16's suppressions sweep — pick when 5b lands.

- [2026-04-25] [commit 5] **HAS_CSV_HEADER architecture rationale (re-surfaced).** User asked: why is HAS_CSV_HEADER only on internal fields, and is it really an "edge"? Answer per architecture spec §11.1: it's an *override* edge — every field can declare one, but most game fields fall back to the default header derivation in the exporter (`v3_<canonical>` prefix). Internal fields are the only fields whose export header doesn't follow the default rule. So "HAS_CSV_HEADER appears only on five fields today" is the spec's expected steady state, not a smell. It's distinct from `RENAMED_FROM` (which encodes *historical* legacy keys, not current export display) — `_date` is the canonical id; `_Date` is the display form; the pre-V2 storage rename `date → _date` will live as a `RENAMED_FROM` edge in commit 10. Three different concerns, three different edge types.

- [2026-04-25] [commit 5] **API-width follow-up scoped as commit 5b interlude.** Commit 4's "engine-method-per-consumer-pattern" rule was the right call for that commit (raw `edgesFrom`/`edgesTo` walks were leaking graph mechanics into UI consumers), but at scale every edge type wants 2–4 named methods on `FieldGraph` and the projected count by commit 15 is ≥25. Commits 6+ are now gated on a `FieldGraph` API-shape ADR (per-edge query modules vs sub-API namespaces vs plugin/registry vs status-quo-with-cap). Captured as commit 5b in the epic with a four-option decision skeleton; the decision-makers should pick before any more vertical slices land, since each unmade-decision day adds method count to refactor.

- [2026-04-25] [commit 5] **Review-process gap.** Pattern-drift, architecture, and code-org agents all approved commit 5's first cut. None flagged: (a) the `INTERNAL_FIELD_SPECS` flatMap indirection vs simpler form, (b) dead code in `internal-field-config.ts` after the csv-exporter fix made `isInternalField(string)` unreferenced, (c) the cumulative API-width trajectory across commits 4 + 5, or (d) the `'internal'` tag vs IS_INTERNAL_FIELD edge duplication. The agents are tuned for per-commit pattern adherence, not for trajectory smells across the epic. Worth thinking about whether the epic's prompt skeleton (`prompt-skeleton.md`) should ask the implementer (or a dedicated agent pass) to explicitly review the cumulative API surface and dead-code drained-by-this-commit at the end of each vertical slice. Not a code change for commit 5 — process feedback for the epic's overall flow.

- [2026-04-25] [commit 5] **Vertical-slice cutover round 2 — `INTERNAL_FIELD_NAMES` deleted.** User feedback after the first round of refactors: "vertical slice means we swap over all code to use this new graph based access." I had left `INTERNAL_FIELD_NAMES` in place on the assumption that 7 storage-boundary consumers using `fields[INTERNAL_FIELD_NAMES.DATE]` were "out of scope" for commit 5. That assumption was wrong — the named `*_NODE` exports already declare those exact strings (`_DATE_NODE.id === '_date'`), so the parallel TS const was pure duplication. Cutover applied to: csv-exporter, csv-parser, data-parser, data-migrations, date-issue-detection (+ its test), csv-import-parsing.test. `INTERNAL_FIELD_NAMES` and `InternalFieldName` deleted. `internal-fields-sync.invariant.test.ts` also deleted (no TS-side const left to sync against — `internal-fields.test.ts` already locks the catalog side). `internal-field-config.ts` is now down to 50 lines (`LEGACY_FIELD_MIGRATIONS` + 2 helpers, all scoped into commit 10 for full deletion). Lesson: future vertical slices should NOT defer "addressing-handle" cleanups to later commits when the canonical replacement already exists. The locked convention is `*_NODE.id` from commit 4's node-identity ADR — use it directly, don't keep parallel TS consts as convenience.

- [2026-04-25] [commit 5] **Epic preamble rewritten with explicit vertical-slice principle (§5) + evolving-epic principle (§7).** §5 spells out: declare the edges + cut over EVERY consumer + delete the legacy mechanism. If too big for one commit, it gets split into `Na` / `Nb` BEFORE starting — silent partial cutovers are the failure mode. §7 makes the epic itself an evolving document — implementers update commit scopes in the same PR if they discover the scope was wrong. Each phase-2 commit (6–14) got a "Cutover requirement" line explicitly listing the legacy mechanism that has to be deleted. Commit 10's scope expanded to include deleting `internal-field-config.ts` entirely (not just shrinking it).

- [2026-04-25] [commit 5] **Prompt-skeleton.md restructured.** Added required reading of the epic preamble (not just the commit's scope), required reading of `Notes-and-findings.md` (running architectural log), and a four-item mid-implementation self-check (cutover audit, cumulative-trajectory check, dead-code drained, epic-evolution check). Report-back format expanded with "Cutover report" and "Trajectory report" sections so the implementer surfaces smells and trajectory concerns proactively rather than waiting for the user to spot them. The user is the decider; the model is the spotter — encoded in the skeleton.

- [2026-04-25] [commit 5] **Architectural exploration spun up: `EXPLORATION-tag-vs-edge.md`** (sibling agent task, may still be in flight as this commit lands). Question being explored: when should a Field node carry an inline `tag` (`['internal']`, `'tournament-only'`, `'user-text'`, `'nullable-empty-string'`, etc.) vs declare a structural edge (IS_INTERNAL_FIELD, CONDITIONAL_ON, …)? Today the catalog has both `tags: ['internal']` AND `IS_INTERNAL_FIELD` edges on the same five fields — the redundancy that surfaced this question. Recommendation TBD when the doc lands. Will inform whether commit 5b (API ergonomics interlude) absorbs the tag cleanup or whether commit 16 (suppression sweep) does it. Don't act on the tag duplication until that decision lands.

- [2026-04-25] [commit 5] **Tag-vs-edge exploration delivered + decision: remove `Node.tags` entirely in commit 5b.** [`EXPLORATION-tag-vs-edge.md`](./EXPLORATION-tag-vs-edge.md) recommended dropping the `'internal'` tag in commit 5; author went further and chose to flat-out remove the `tags` concept from `Node` in commit 5b. Rationale: edges are structurally queryable; tags are unstructured node-local strings. At our scale, every fact a consumer queries should be an edge — tags would be a parallel, less-powerful representation. Forward implications: planned `'tournament-only'` tag is dropped (covered by `CONDITIONAL_ON` in commit 13); `'user-text'` and `'nullable-empty-string'` (spec §11.4 gotchas) become edges in commits 8 / 14. New guideline going into [`field-graph-for-ai.md`](./field-graph-for-ai.md) when 5b lands: "Never introduce `Node.tags`. Facts about a node are edges." Commit 5b's scope updated to include the type/builder/test/declaration deletions.

- [2026-04-25] [commit 5] **Variable-swap is not cutover — surfaced via the csv-exporter ladder.** Round 2 of the cutover replaced `INTERNAL_FIELD_NAMES.DATE` with `_DATE_NODE.id` in the if/else ladders inside `csv-exporter.ts` (lines 138–156, 320–339). The user's review caught that this is just porting the strings; the ladder itself — five branches each with bespoke fallback logic (`run.fields[X]?.rawValue || formatIsoDate(run.timestamp)`, `encodeNotesForStorage(...)`, `|| run.runType`, etc.) — is exactly the conditional logic the graph is supposed to eliminate. The cutover delivered the letter, not the spirit, of the architecture. Decision: the conditional-logic elimination is a different concept (export-time field-aware extractors) — added as **commit 5c** in phase 2.5 (`HAS_CSV_EXTRACTOR` edge + registry, mirroring commit 9's parse-time `IS_DERIVED_FROM` + DERIVERS pattern). Commit 5 leaves the ladder intact with this Notes-and-findings entry as the explicit deferral. Lesson encoded in the prompt skeleton: a new self-check ("conditional-logic elimination") and a new report-back section ("conditional-logic report") so future commits surface this class of mistake proactively.

- [2026-04-25] [commit 5] **Phase 2.5 now has two interludes (5b + 5c), both gating commits 6+.** Two architectural concerns surfaced from commits 4 + 5: (a) `FieldGraph` API is heading toward a god object with ≥25 methods by commit 15, (b) cutovers risked landing as variable-swaps without eliminating conditional logic. 5b owns API shape + tags removal; 5c owns the field-aware extractor concept. Commit 6's dependencies updated to require both. Total epic count grew from 16 to 17 commits. Risk of further interludes is real — every vertical slice may surface new architecture-trajectory issues — and the prompt skeleton now bakes in the "epic is evolving" mindset so the model surfaces them rather than the user.

- [2026-04-25] [commit 5] **Exploration-doc convention added: every exploration doc carries a structured `## Human decision` section near the top.** The model's recommendation goes under "Recommendation summary (30-second read)" (renamed from "Decision summary"); the human's actual decision goes in `## Human decision` directly below the front-matter, including the decision sentence, the human's reasoning (preserved as a direct quote where possible — the conversational voice is load-bearing context for future revisits), explicit deviations from the recommendation, scope (which commits implement it), status, and future revisit triggers. Lightweight ADR pattern, no separate ADR tracker. Applied retroactively to [`EXPLORATION-node-identity-abc-deep-dive.md`](./EXPLORATION-node-identity-abc-deep-dive.md) (the only other exploration doc) and prospectively to [`EXPLORATION-tag-vs-edge.md`](./EXPLORATION-tag-vs-edge.md). [`prompt-skeleton.md`](./prompt-skeleton.md) gained an "Exploration-doc convention" section with the placeholder template — every future exploration doc spawned during an implementation chat MUST include the section. Why this matters: the model's recommendation is the *input* to the decision; the human's decision is the *output*. Both stay in the doc — preserving the reasoning chain in git history so future revisits don't lose the discussion / consideration / justification context that drove the original choice.

- [2026-04-25] [commit 5b] **`Node.tags` removed from the type system.** Per the human decision recorded in [`EXPLORATION-tag-vs-edge.md`](./EXPLORATION-tag-vs-edge.md), `tags?: readonly string[]` is gone from `Node` (`types.ts`), the `tags` option is gone from `NodeOptions` in `builders.ts`, and the five `{ tags: ['internal'] }` arguments are gone from `_DATE_NODE` / `_TIME_NODE` / `_NOTES_NODE` / `_RUN_TYPE_NODE` / `_RANK_NODE`. The two pre-existing tag-introspection assertions in `fields.nodes.test.ts` were rewritten to call `appGraph().isInternalField(node)` — they now assert the bidirectional invariant "underscore-prefix ↔ IS_INTERNAL_FIELD edge" using the engine API instead of node-local string matching. Audit confirmed zero external `Node.tags` consumers (grep across `src/` showed all 9 hits inside the field-graph directory itself). New guideline appended to [`field-graph-for-ai.md`](./field-graph-for-ai.md): *"Never introduce `Node.tags`. Facts about a node are edges."* Future facets (`'tournament-only'`, `'user-text'`, `'nullable-empty-string'`, `'pending_classification'`) will land as edges per their respective owning commits — the tag-vs-edge exploration's per-commit table is the working plan.

- [2026-04-25] [commit 5b] **Engine API ergonomics ADR landed: [`EXPLORATION-engine-api-shape.md`](./EXPLORATION-engine-api-shape.md).** Recommendation is **Option A (per-edge query modules)** — each `catalog/*.edges.ts` ships a sibling `*.queries.ts` exporting plain functions; `FieldGraph` reduces to a stable primitives layer (`edgesFrom`, `edgesTo`, `edgesOfType`, `nodesOfKind`, `getField`, `resolveFieldByAnyKey`, `terminalOf`, `toId`); top-level barrel re-exports thin wrappers that bind to `appGraph()` so consumers write `csvHeaderOf(_DATE_NODE)` (Style 2) instead of `appGraph().csvHeaderOf(_DATE_NODE)`. The doc analyzes Options B (sub-API namespaces), C (plugin/registry), and D (status quo + lint cap) and explains why A wins on the trade-off matrix (44 vs 31 vs 30 vs 25). **The refactor is explicitly deferred to a proposed commit 5d** per the epic DoD's deferral option — keeps the ADR commit small enough to review on paper before churning 14 method bodies and 3 production consumers. Human decision section is the placeholder template; awaiting human review. Commit 6+ are still gated on this ADR resolving (per epic's phase-2.5 framing). If the human picks Option D (status quo), the epic does not need a 5d commit; commits 6+ continue adding methods to `FieldGraph` per the engine-method-per-consumer rule.

- [2026-04-25] [commit 5b] **Trajectory note: engine API surface today is smaller in practice than in projection.** Production code paths that currently call `FieldGraph` methods are limited to 3 files (`csv-exporter.ts`, `run-type-display.ts`, `run-type-filter.ts`) calling 3 distinct methods (`internalFields`, `csvHeaderOf`, `enumValueMeta`). The engine class has 14 methods because each future commit anticipated needing one; in reality most are exercised only by tests today. This is fine — the methods are cheap to declare — but it argues for keeping the refactor cost low (few real consumer migrations needed in commit 5d if Option A is picked).

- [2026-04-25] [commit 5b] **Proposed commit 5d added to the epic candidate list (not yet inserted).** If the human picks Option A or B in the engine-API ADR, commit 5d implements that decision: refactor the 14 existing methods into the chosen shape and migrate the 3 production consumers. If the human picks D, commit 5d is not needed. Did NOT pre-insert 5d into the epic — the human's decision in `EXPLORATION-engine-api-shape.md` should drive the epic edit, not the model's anticipation. Implementer of 5d (whether human or AI) should add the commit entry in the same PR that lands the refactor.

- [2026-04-25] [commit 5b] **Cutover semantics for an ADR-only commit.** Commit 5b is a "decision-shaped" commit per the epic preamble; the cutover principle still applies but maps to "remove the legacy mechanism" only for the parts the human has already decided. `Node.tags` was the resolved part — its removal is a complete cutover (no parallel representation remains). The API ergonomics decision is the unresolved part — by design, no engine code changes here. This is the explicit "ADR + Node.tags" scope the epic carved out; the API refactor is the deferred follow-up. Reporting both halves under the same commit's "cutover report" makes the distinction visible.

- [2026-04-25] [commit 5b] **Engine-API ADR refined after human review — Option A + Style 2 confirmed; commit 5d scope expanded.** Human review pass on [`EXPLORATION-engine-api-shape.md`](./EXPLORATION-engine-api-shape.md) accepted Option A (per-edge query modules) + Style 2 (singleton-bound consumer ergonomics) as the recommended direction and ruled out B / C / hybrid. Five open questions raised during review and now baked into the doc as a new "Update — refinements from human review" section: (a) testing under Style 2 — integration-style is fine, don't mock the singleton, verify mechanism not data; (b) singleton temporal coupling — none, `appGraph()` is already lazy; (c) barrel imports — overstated concern, downgraded; (d) AI cold-start discoverability — multi-pronged: query index in `field-graph-for-ai.md` + glob conventions + future CLI; (e) directory shape — delegated to `code-organization-naming` agent. Agent recommended `catalog/edges/<concept>/` per-concept subdirectories with suffix-preserved filenames (`internal-fields/internal-fields.edges.ts`, etc.) so `**/*.queries.ts` glob discoverability survives the directory restructure. Cross-concept edges (HAS_DISPLAY_NAME, HAS_COLOR) live under purpose-named folders like `edges/presentation/`.

- [2026-04-25] [commit 5b] **Commit 5d absorbed into 5b — full refactor landed in one commit.** Human direction: *"let's do all that in the scope of this commit."* The originally-deferred 5d work (engine slim-down, query module creation, directory restructure, consumer migration, query-index docs) shipped inside 5b alongside the ADR + `Node.tags` removal. Result: one bigger commit but a clean architectural moment — the decision and its embodiment ship together with no half-state across PRs. Total epic count back to 16 commits (5d removed from candidate list).

- [2026-04-25] [commit 5b] **As-shipped surface — `FieldGraph` slimmed from 14 query methods to 8 primitives (~150 LOC engine class).** Public methods now: `getField`, `resolveFieldByAnyKey` (parser-boundary string-only); `toId`, `terminalOf`, `edgesFrom`, `edgesTo`, `edgesOfType`, `nodesOfKind` (FieldRef-polymorphic primitives). All domain queries live in `catalog/edges/<concept>/<concept>.queries.ts` modules. The top-level barrel `src/shared/domain/field-graph/index.ts` exports 15 Style-2 singleton-bound wrappers (one thunk per query) so consumers write `csvHeaderOf(_DATE_NODE)` instead of `appGraph().csvHeaderOf(...)`. Five concept directories landed: `internal-fields/`, `enum-values/`, `sections/`, `sources/`, `presentation/`. Sections + sources + presentation are queries-only today (their edges land in commits 6, 7, and 8/12 respectively); their query functions are ready to consume those edges when they land.

- [2026-04-25] [commit 5b] **Two test shapes locked: `*.queries.test.ts` for behavior, `*.invariants.test.ts` for catalog shape.** Behavior tests build a small fixture graph with `new FieldGraph(...)` and call queries explicitly with `(graph, ...)` (Style 1). Invariant tests run against the production catalog using the singleton-bound queries (Style 2). The split makes the testing intent visible from the filename: a query failure in `*.queries.test.ts` is a mechanism bug; a failure in `*.invariants.test.ts` is a catalog-shape drift. Engine-primitive tests + invariant tests stay in `field-graph.test.ts` (the 13 tests for moved domain queries migrated to the per-concept folders).

- [2026-04-25] [commit 5b] **Cold-start query index added to [`field-graph-for-ai.md`](./field-graph-for-ai.md).** A 17-row table — query name, return type, `Use when` description — is the immediate answer to "what queries exist?" for any AI agent (or human) walking into the codebase cold. New "Adding a query method" section walks through the 6-step convention (find concept folder → add queries function → add test → add bound wrapper → update query index → don't add to FieldGraph). The "Adding a new field" / "Renaming a field" / "Adding a new relationship type" sections updated for the new `catalog/edges/<concept>/` directory layout. Future work (CLI tooling — `graph:list-queries`, `graph:describe-edge`, `graph:explain`) is deferred per human direction (*"I'm going to leave discoverability for later"*); the manually-maintained index + glob conventions cover the immediate need.

- [2026-04-25] [commit 5b] **All three production consumers migrated to Style 2 (singleton-bound).** [`csv-exporter.ts`](../../src/features/data-export/csv-export/csv-exporter.ts): `appGraph().internalFields()` → `internalFields()`; `graph.csvHeaderOf(field)` → `csvHeaderOf(field)`. [`run-type-display.ts`](../../src/shared/domain/run-types/run-type-display.ts): `appGraph().enumValueMeta(...)` → `enumValueMeta(...)`. [`run-type-filter.ts`](../../src/features/analysis/shared/filtering/run-type-filter.ts): same pattern. Two test files also migrated (`fields.nodes.test.ts`, `enum-sync.invariant.test.ts`). Net call-site shape: strictly fewer characters per call than pre-refactor, no temporal-coupling burden because `appGraph()` is already lazy + cached.

- [2026-04-25] [commit 5b] **Directory restructure shape: nodes flat, edges per-concept.** `catalog/*.nodes.ts` files (6 files, slow growth) stay flat at the catalog root. All edges + queries + tests moved into `catalog/edges/<concept>/` per-concept subdirectories. Suffix preserved on filenames (`internal-fields/internal-fields.edges.ts`, not `internal-fields/edges.ts`) so the `**/*.queries.ts` glob remains a one-call discoverability primitive — directory carries the concept; filename carries the kind. Two-level aggregator: `catalog/edges/index.ts` rolls up every concept's `*_EDGES`; `catalog/index.ts` consumes that and adds node aggregations. Convention captured in `field-graph-for-ai.md` for future commits 6+ — every new edge concept gets its own `catalog/edges/<concept>/` folder; never add files at the catalog root for moved concepts.

- [2026-04-25] [commit 5b] **Vite import-resolution race did NOT trip this run — first time on this branch.** Prior runs on commit 4 + commit 5 + commit 5b's first round saw 101 file-level transform failures (with all 1654 test cases passing). This run: `Test Files: 219 passed (219)`, `Tests: 3170 passed | 1 skipped`. The race appears intermittent and may have been alleviated by the directory restructure (more files, more spread-out imports?) — or it's just timing variance. Worth re-checking on the next run to confirm whether the issue has actually resolved or is just dormant.

- [2026-04-25] [commit 5b] **E2E verified end-to-end as part of integration-precheck.** `npm run integration-precheck` ran lint → type-check → 219 unit-test files (3170 tests, 1 skipped) → 11 E2E specs (5 skipped — pre-existing migration-era ledger entries; none added by this commit). First commit on this branch where the full chain ran green without intervention.

- [2026-04-25] [commit 5b] **`PATTERN.md` convention introduced — separates pattern definition from instance comments and from pattern history.** Human review feedback: instance-file comments were regurgitating Style 2 / EXPLORATION-* references that only mattered to whoever was *evolving* the pattern, not to consumers using existing instances or contributors stamping out new instances. The chef's-knife framing: three readers (user / instance maker / pattern designer) want three different information surfaces; mixing them everywhere created noise. Resolution: created `src/shared/domain/field-graph/catalog/edges/PATTERN.md` as the single source of truth for the per-concept-directory pattern, with sections covering "what's a concept," "files inside a concept folder," "how queries are written," "two test shapes," "how to add a new concept folder," "how to add a new query to an existing concept," "how to add a new edge type," "cross-source-kind edges," "what does NOT belong," "anti-patterns to avoid," and an "Appendix — pattern history" linking the EXPLORATION-* docs. Stripped all pattern-evolution prose and Style 2 / EXPLORATION references from the per-instance files (engine, queries modules, top barrel, edges aggregator, test headers); kept only what's non-obvious to a reader of that specific file (load-bearing declaration order in internal-fields.edges.ts; the override-only nature of csvHeaderOf; the cross-source-kind rationale for presentation/). `field-graph-for-ai.md` collapsed: the cold-start query index stays (usage info), but "Adding a new query / field / relationship type" sections now point at `PATTERN.md` instead of duplicating it.

- [2026-04-25] [commit 5b] **Meta-pattern added to `.ruler/04-engineering-standards.md` — "Pattern definition files (`PATTERN.md`)."** Codifies the principle for future patterns: when introducing a code-organization pattern future contributors will replicate, drop a `PATTERN.md` at the locality of the pattern. Defines the three reader audiences (user / instance maker / pattern designer), the three sections each `PATTERN.md` should carry (what / how-to-add / what-does-not-belong + history appendix), and the discipline that instance-file comments must NOT regurgitate the pattern definition or reference design history (those references belong in the appendix). Existing-`PATTERN.md` examples list seeded with the field-graph one. Future patterns introduced by other epics get the same treatment automatically — when an AI agent or human is asked to add a new code-organization pattern, this rule kicks in.

- [2026-04-25] [commit 5b] **Reader-by-purpose discipline now drives comment density.** Removed JSDoc lines like "True when `field` carries an IS_INTERNAL_FIELD marker edge" — function name + return type already convey that. Kept comments only when they add information unavailable from name + types: "Override only — most fields don't declare HAS_CSV_HEADER" (non-obvious fallback expectation), "Exact-string match. Callers needing case-insensitive matching normalize before calling" (non-obvious matching policy), "Multi-section is allowed (a field can belong to several sections)" (non-obvious cardinality). The principle scales — every future query function should pass the test "does removing this comment confuse a future reader?" before being written.

- [2026-04-25] [commit 5c] **`HAS_CSV_EXTRACTOR` shipped — both csv-exporter ladders gone.** New terminal-string edge type (cardinality `'one'`, `Field → string`) names a registered extractor in the new `CSV_EXTRACTORS` registry. The two 5-branch `if/else if (fieldName === _X_NODE.id)` ladders in [`csv-exporter.ts`](../../src/features/data-export/csv-export/csv-exporter.ts) (one in `detectDelimiterConflicts`, one in `exportToCsv`'s row build loop) collapsed to a single shared helper `extractAppFieldValue(fieldName, run)` that does one graph query (`csvExtractorOf`) plus one registry lookup. Reading `csv-exporter.ts` no longer surfaces any per-internal-field knowledge — the field id appears once per extractor in the registry, never in the consumer.

- [2026-04-25] [commit 5c] **Registry placement decision: graph-layer, ParsedGameRun-typed.** `CSV_EXTRACTORS` lives at [`catalog/edges/internal-fields/internal-fields.csv-extractors.ts`](../../src/shared/domain/field-graph/catalog/edges/internal-fields/internal-fields.csv-extractors.ts) per the epic's "co-located with the catalog" guidance, with extractor signature `(run: ParsedGameRun) => string` per the epic scope's literal type. This is a new domain coupling — graph internals previously knew nothing about `ParsedGameRun`. Justification: two of the five extractors (`date`, `runType`) need run-level fallbacks (`run.timestamp`, `run.runType` cached property) that aren't reachable through `run.fields` alone. The alternative — pushing those fallbacks back into the consumer — would have re-introduced the conditional logic the commit was supposed to eliminate. Containment: the coupling lives in one concept folder, not the engine. Re-evaluate if commit 9's DERIVERS coupling differs (spec §11.3 uses a field-only signature for derivers, so they can stay app-state-agnostic; export-time extractors are uniquely run-shaped).

- [2026-04-25] [commit 5c] **Trajectory: zero engine methods added.** `FieldGraph` public surface stays at 8 methods (parser-boundary `getField` / `resolveFieldByAnyKey`; primitives `toId`, `terminalOf`, `edgesFrom`, `edgesTo`, `edgesOfType`, `nodesOfKind`). The new `csvExtractorOf(graph, field)` query lives in [`internal-fields.queries.ts`](../../src/shared/domain/field-graph/catalog/edges/internal-fields/internal-fields.queries.ts) per 5b's per-concept module convention, and the singleton-bound wrapper in the barrel. The 5b restructure paid off — adding a new edge type's full query surface didn't require touching the engine class.

- [2026-04-25] [commit 5c] **Test shapes: registry + invariant + behavior.** Three new test slots: (a) [`internal-fields.csv-extractors.test.ts`](../../src/shared/domain/field-graph/catalog/edges/internal-fields/internal-fields.csv-extractors.test.ts) covers per-extractor behavior (rawValue path + fallback path) — exercises the registry functions directly with crafted `ParsedGameRun` fixtures; (b) [`internal-fields.invariants.test.ts`](../../src/shared/domain/field-graph/catalog/edges/internal-fields/internal-fields.invariants.test.ts) gained two assertions: every internal field has a `HAS_CSV_EXTRACTOR` edge AND every declared extractor name resolves to a registered function (catches drift if someone adds an edge but forgets the registry entry); (c) [`internal-fields.queries.test.ts`](../../src/shared/domain/field-graph/catalog/edges/internal-fields/internal-fields.queries.test.ts) gained a `csvExtractorOf` describe with the canonical "auto-routes a new field via the declared extractor name" test that proves adding an edge + registry entry needs zero exporter changes. Round-trip coverage for the refactored ladders comes from the existing `csv-persistence.test.ts` round-trip suite (canonical and locale variations), all green.

- [2026-04-25] [commit 5c] **Forward note for commit 9 (DERIVERS).** The same edge-name + registry shape this commit established (`HAS_CSV_EXTRACTOR` → `CSV_EXTRACTOR_NAMES.X` → `CSV_EXTRACTORS[name]`) is the template for commit 9's `IS_DERIVED_FROM` + `DERIVERS` registry. Differences: derivers per spec §11.3 take `Record<string, GameRunField | undefined>` (no run-level shape), so they can live in graph layer without coupling to `ParsedGameRun`. Commit 5c's optional registry-consolidation idea ("Commit 9 will likely consolidate both registries") is unlikely to land literally — different signatures, different domain coupling — but the *naming convention* (`csv-extractor:X` for output, `deriver:X` for input) and the *file co-location* (registry next to edges in the same concept folder) carry over.

- [2026-04-25] [commit 5c] **Vite import-resolution race re-surfaced.** First `npm run integration-precheck` of this commit hit 114 file-level transform failures (with `1579 passed | 0 failed` actual test cases) — the same pre-existing intermittent race tracked in the commit-4 / commit-5b notes. Second run was clean (`220/220 files passed, 3185 tests passed, 1 skipped`). 5b's directory restructure did not actually resolve the race; it was dormant for one run. Worth keeping under observation but still not in scope for any specific commit.

- [2026-04-25] [commit 5c] **No suppressions added.** Zero new `eslint-disable`, `test.skip`, `@ts-expect-error`, or config overrides. Migration-era ledger unchanged.

- [2026-04-25] [commit 5c→8] **Commit 5c was retired mid-implementation; folded into commit 8.** The first 5c pass shipped a `HAS_CSV_EXTRACTOR` edge + `CSV_EXTRACTORS` registry. Human review surfaced the deeper architectural question — *"this isn't really an extractor concern; the underlying axis is data type, and the graph should drive that for every field, not just internal ones."* Redesign captured in [`EXPLORATION-data-type-edge-vs-property.md`](./EXPLORATION-data-type-edge-vs-property.md). Outcome: every Field declares `IS_OF_TYPE` (renamed from `HAS_DATA_TYPE`); parser becomes graph-driven; csv-exporter ladder collapses via uniform `formatFieldValue` dispatch reading the graph-set `field.dataType`. The 5c-as-shipped code (the registry + the `HAS_CSV_EXTRACTOR` edge type) was reverted before this commit landed; only the architectural lessons survive.

- [2026-04-25] [commit 8] **The `_runType=0` bug — story worth preserving.** The first attempted ladder-elimination refactored csv-exporter to dispatch by `field.dataType` uniformly. Bulk-import E2E started failing: `_runType` exported as `'0'` instead of `'farm'`, all tabs showed `(0)` after reload. Root cause: `field-utils.ts`'s `getFieldConfig` lowercased then exact-matched the original CSV header. For column `_Run Type` (with space), `'_run type'` didn't match `EXACT_FIELD_CONFIGS['_run_type']` (with underscore). Fell through to default `'number'`. `formatFieldValue` with `dataType='number'` and `rawValue='farm'` computed `numValue=0` and returned `'0'`. The OLD per-field switch didn't trip on this because it returned `rawValue` directly without dispatching by dataType. **The bug exposed why parser + exporter need to agree on dataType, and why label-pattern-matching is a fragile substrate.** Graph-driven dispatch fixes it: `IS_OF_TYPE _runType → 'string'` is authoritative; the parser sets `field.dataType='string'`; the exporter returns rawValue. Reachable from any V2-shaped column header that doesn't exactly match the EXACT_FIELD_CONFIGS table — a class the graph closes structurally.

- [2026-04-25] [commit 8] **Cardinality stayed `'one'` for `IS_OF_TYPE` despite the spec calling for `'at-least-one'`.** Engine cardinality `'at-least-one'` would have failed every fixture test in the suite that creates a Field node without declaring its type (sections, sources, presentation, internal-fields, enum-values, field-graph engine). Production-catalog universality is enforced by an explicit invariant test in [`data-types.invariants.test.ts`](../../src/shared/domain/field-graph/catalog/edges/data-types/data-types.invariants.test.ts) (iterates `appGraph().nodesOfKind('Field')` and asserts every one has `dataTypeOf`). Same end-state production guarantee, no fixture tax. Documented at the EDGE_META declaration site in [`types.ts`](../../src/shared/domain/field-graph/types.ts:101). If the engine ever grows a "validation context" that distinguishes production graphs from fixture graphs, flip back to `'at-least-one'`.

- [2026-04-25] [commit 8] **`battleReport_killedBy` was mis-typed as `'number'` pre-graph.** The OLD `EXACT_FIELD_CONFIGS` had `'killed by': 'string'` for the V2 display label, but the V3 canonical key `battleReport_killedBy` lowercased to `'battlereport_killedby'` which didn't match — fell through to `'number'`. Several string-typed consumers (tier-stats, source-analysis) treated it as numeric. Graph cutover (`IS_OF_TYPE battleReport_killedBy → 'string'`) corrects this. Latent for years — surfaced because the cutover forced explicit typing of every field. Worth flagging that pattern-matching label heuristics can mask drift like this indefinitely.

- [2026-04-25] [commit 8] **Catalog-level [`PATTERN.md`](../../src/shared/domain/field-graph/catalog/PATTERN.md) introduced.** Sits one level above the existing per-concept-edge `edges/PATTERN.md`, scoped to "what's a node, what's an edge, the four-question litmus for edge-vs-node-property, the `Node.payload` carve-out." The litmus reproduces every prior structural decision (IS_INTERNAL_FIELD, HAS_CSV_HEADER, BELONGS_TO_SECTION, ACCEPTS_VALUE, RENAMED_FROM, planned SHIPPED_IN_SCHEMA / PARTICIPATES_IN_COMPOSITE_KEY) when applied retroactively. Cross-linked from [`field-graph-for-ai.md`](./field-graph-for-ai.md). The user explicitly opted to commission it now (rather than defer to a follow-up commit) so the next vertical slice doesn't re-litigate edge-vs-node-property choices.

- [2026-04-25] [commit 8] **Edge-name rename `HAS_DATA_TYPE` → `IS_OF_TYPE`.** The `HAS_X` shape implies "may or may not have X" (the right framing for `HAS_CSV_HEADER`, which is genuinely an override). For data type, every catalog Field is universally typed — there's no "has" question, only "is." `IS_OF_TYPE` matches the universal-by-invariant semantic and parallels existing `IS_INTERNAL_FIELD` / `IS_SOURCE_OF`. ~30-line mechanical grep-and-replace. The concept folder stayed `data-types/` per `edges/PATTERN.md`'s "purpose-named folders, not edge-named ones" rule — the folder describes the concept (data types as a domain), not the edge type.

- [2026-04-25] [commit 8] **TypeScript-vs-graph trade-off — captured as a revisit trigger, not resolved.** Per [`EXPLORATION-data-type-edge-vs-property.md`](./EXPLORATION-data-type-edge-vs-property.md) §5, the user's deeper concern: the graph is becoming the metadata source of truth, displacing TypeScript's role as "what shape is this entity?" — and they're not sure if the trade-off is worth it. Acknowledged and parked. Commit 16's litmus retrospective + TypeScript-vs-graph audit will close the loop after the epic completes.

- [2026-04-25] [commit 8] **Process feedback: prompt skeleton flipped to "mark commit DONE on initial pass."** Per the user's voice-note feedback, future commits default to flipping the EPIC checkbox to `[x]` and `Status: TODO → DONE` on initial implementation, with a documented exception for "material architectural question that might restructure the commit." This commit (8) takes the new convention live — the previous "wait for human verification" rule was producing friction without value.

- [2026-04-25] [commit 8] **The transitional preprocessor in csv-exporter is the one remaining smell.** `withPopulatedAppFields(run)` populates `_date` / `_time` / `_runType` from cached run properties when the field is missing, and pre-encodes `_notes` for tab-delimited CSV. Three of these (the cached-property fallbacks) are commit 9's territory — the IS_DERIVED_FROM cascade ensures the fields are populated at parse time, removing the need. The fourth (notes encoding) is its own concern — needs an edge or a `'user-text'` data-type variant; flagged in commit 9's expanded scope as a follow-up if it doesn't fit naturally there.

- [2026-04-25] [commit 8] **`legacyTypeFallback` in field-utils.ts is a transitional shim.** When `dataTypeOf(deriveCanonicalKey(originalKey))` returns undefined (V2 display labels like `'Real Time'` or `'Killed By'` whose canonical id is `realTime` / `killedBy`, not in the graph), a small substring-pattern fallback runs to preserve V2 import behavior. Commit 10 (`RENAMED_FROM` edges) absorbs this entirely — once `realTime` is a `RENAMED_FROM battleReport_realTime`, `dataTypeOf` resolves through the rename and the fallback never fires. Marked with a deletion-owner comment in the source.

- [2026-04-25] [commit 10] **`RENAMED_FROM` shipped — single rename mechanism, all consumers cut over.** New concept folder `catalog/edges/renames/` declares 153 RENAMED_FROM edges (7 V1→V2 internal-field renames at `schema:v2`; 146 V2→V3 game-field renames at `schema:v3`). The legacy mechanisms it replaces are gone: `V2_TO_V3_FIELD_MAP` (220-line hand-authored map), `v2-to-v3-field-map.generated.ts`, `internal-field-config.ts` (LEGACY_FIELD_MIGRATIONS / isLegacyField / getMigratedFieldName), and the duplicate `LEGACY_FIELD_MIGRATIONS` in `data-migrations.ts`. Every consumer (csv-parser, csv-field-mapping, data-parser, data-migrations.migrateRunsV1ToV2, v2-to-v3-migrator, remap-v2-field-keys) routes through a single graph call: `resolveFieldByAnyKey(rawKey)` returns canonical Node or null. Engine machinery (legacy-key reverse index, payload validation, RENAMED_FROM cardinality) was already in place from commit 1 — this commit only added edge declarations + cutover. The architectural payoff is significant: anyone adding a future rename writes one `rename(NEW_NODE, 'oldKey', SCHEMA_VX_NODE)` line in one file; no more "where does this map live again?"

- [2026-04-25] [commit 10] **`legacyTypeFallback` deleted; `getFieldConfig` is graph-only.** Pre-commit, `getFieldConfig` had a fallthrough that pattern-matched display labels ("Real Time" → 'duration', "Killed By" → 'string'). Post-commit, `dataTypeOf(resolveFieldByAnyKey(camel)?.id ?? camel) ?? 'number'` covers every case the fallback handled — RENAMED_FROM edges resolve V2 display labels through to the V3 canonical, which carries the `IS_OF_TYPE` edge. Same end-state types, no heuristic. The "tier with '+' is string" special case moved to `extractKeyStatsFromFields` as an unconditional regex extract of the leading integer (works for both V28 plain `"10"` and V2 tournament `"10+"`).

- [2026-04-25] [commit 10] **`deriveCanonicalKey` learned about the V3 storage prefix.** CSV round-trip uses `v3_<canonical>` as the column header (e.g. `v3_realTime`). Pre-commit, type detection ran on the raw header → `toCamelCase('v3_realTime')` → `'v3Realtime'` → no graph hit → fell back to `legacyTypeFallback` (substring "time" → 'duration'). Post-commit with `legacyTypeFallback` deleted, that fallback is gone — so `deriveCanonicalKey` now strips the `v3_` prefix first. Stripping this in deriveCanonicalKey (one place) was lower-impact than threading the canonical key through `createGameRunField` (would have touched parser internals).

- [2026-04-25] [commit 10] **One legacy spelling ambiguity surfaced via the build invariant: `goldenBot` (V2 bare) was claimed by both `coins_goldenBot` and `killedWithEffectActive_goldenBot`.** The original V2_TO_V3_FIELD_MAP only mapped `goldenBot → killedWithEffectActive_goldenBot` (best-effort guess from the disambiguation prep). Followed that mapping. The graph builder's `checkLegacyKeyUnique` would have caught it loud if I'd accidentally double-claimed. Same held for `goldenTower` (mapped to `killedWithEffectActive_goldenTower`, NOT `coins_goldenTower` — even though the V2 column probably carried more `coins_goldenTower` semantic; the prior decision stood). These are V2 last-write-wins ambiguities that the V3 migration's pre-migration backup recovers from if a user notices wrong-bucket data.

- [2026-04-25] [commit 10] **`migrateRunsV1ToV2` semantics broadened with the cutover (intentional).** Pre-commit it was a narrow V1-only rename (`date` → `_date`, etc.). Post-commit, since `resolveFieldByAnyKey` knows ALL legacy keys (V1 internal + V2 game), the function now also renames V2 game keys (`tier` → `battleReport_tier`) on its way through. The function name is now slightly misleading — the operation is really "canonicalize all field keys via the graph" — but renaming the function is out of scope for this commit. Tests updated to reflect the broader behavior. This is actually closer to what users with V1 data need: a single hop from "any historical key" → "current canonical."

- [2026-04-25] [commit 10] **Resolution-boundary invariant test deferred (not added).** Architecture spec §14.4 shows a test that greps `src/` for `resolveFieldByAnyKey` outside the parser/import boundary. Verified manually post-cutover — all 13 references are at the parser boundary, in field-graph internals, or in tests. Did NOT codify as an invariant because (a) the per-file allow-list shape encoded in the spec is brittle (any reorganization re-paths the allow-list); (b) the current callers form a small natural set easy to monitor by hand. Worth reconsidering at commit 16 alongside the litmus retrospective if false leaks happen.

- [2026-04-25] [commit 10] **Dead code drained.** `extractTimestampFromFields` (in field-utils.ts) reads `fields.date` / `fields.time` (V1 keys). Post-cutover those keys never reach this function — `remapV2FieldKeys` renames them at the parser boundary, so app state is always canonical. Function returns `null` for any current input. Did NOT delete in this commit (out of scope; one production caller in `use-data-input-form.ts`); flagging as candidate for commit 16's suppression sweep / dead-code drain pass. Same for `migrateCsvOnImport` and the now-test-only export of `migrateRunsV1ToV2` — `migrateDataIfNeeded` is the only production-load-path migration entry; the others survive only for tests.

- [2026-04-25] [commit 10] **Catalog-PATTERN.md litmus stayed quiet — RENAMED_FROM was already an edge.** No cases this commit where I had to choose between a `Node.payload` entry and an edge. RENAMED_FROM is canonical-edge by structure (legacy keys are queried, so they fail question 1 of the litmus immediately). The mild ambiguity was *which payload fields RENAMED_FROM carries* (`legacyKey` + `atSchema` + optional `reason`) — those are the per-edge facts, not per-node facts. Spec was clear; no exploration needed.

- [2026-04-25] [commit 10 review round] **Material architectural question surfaced — `EXPLORATION-parser-boundary-resolution.md` spun up.** User code review observed that even after the cutover, *string-shape normalization* still lives in four files (`field-utils.ts:deriveCanonicalKey`, `csv-parser.ts:buildColumnToFieldMap`, `csv-field-mapping.ts`, `v2-to-v3-migrator.ts:classifyV2Header`). Each strips `v3_` prefix, handles `_underscore`, camelCases the rest *before* asking the graph. This duplication is exactly what the graph is supposed to subsume. The architectural tension: should `resolveFieldByAnyKey` accept any input string and return canonical, or is the parser-boundary normalization legitimately caller-side? Captured as a 4-option exploration; recommended Option A (centralize into engine, build HAS_DISPLAY_NAME + HAS_CSV_HEADER reverse indexes, strip schema-driven storage prefix internally) but flagged the display-label collision concern (`'Black Hole'` appears in 4 sections — the reverse index can't be cleanly one-to-one). **Per the prompt-skeleton's material-question rule, commit 10 was unmarked and held open** until the human decision in the exploration doc lands.

- [2026-04-25] [commit 10 review round] **Polish fixes from same review:** (a) deleted the leaky breadcrumb comment in `data-parser.ts` that explained graph mechanics in a consumer file (violated the consumer-comment rule from `field-graph-for-ai.md` — my own rule); (b) removed `legacyKeysOf` and `renamesOf` from the top-level barrel since exposing them invites consumer code to reason about renames (kept in the per-concept queries module for tests + future debug tooling); (c) added two-way breadcrumbs — every transitional location in `src/` now has a `TRANSITIONAL — to be deleted by commit X` comment, AND commit X's epic entry references the specific files. Going forward, breadcrumbs are bi-directional by default during multi-commit work.

- [2026-04-25] [commit 10 review round] **Tier-`+` parsing routed to commit 9.** User flagged that the leading-int regex extraction is duplicated across `data-parser.ts:extractKeyStatsFromFields` and `run-type-detection.ts:extractNumericStats`. Both files now carry transitional comments referencing commit 9. Commit 9's scope expanded to absorb tier-`+` parsing as either a self-deriver, a dedicated `'tier'` data-type, or a parser-level fix — decision to be made when commit 9 starts. The deeper architectural framing (per spec §11.4 gotcha 4): `_runType IS_DERIVED_FROM battleReport_tier { deriver: 'runTypeFromTier' }` already covers the run-type detection half; tier parsing itself is a separate concern that the data-type system is the natural home for.

- [2026-04-25] [commit 10 review round] **Process feedback that informs every multi-commit refactor.** User: *"as you're modifying these files and it's going to be refactored in the future or another commit, reference that commit in these files and then also add a breadcrumb to this file in the file that the commits going to deal with this so kind of have like a two-way mapping."* This is now the default discipline for the rest of the epic — when a commit leaves transitional state behind, the breadcrumb is bi-directional: the source file says "deleted by commit N," and commit N's epic entry says "deletes file X / file Y / file Z." Existing breadcrumbs (commit 8's deferral notes for `legacyTypeFallback`, `withPopulatedAppFields`, etc.) followed the pattern partially; codifying it explicitly here so future commits don't drop the back-link.

- [2026-04-25] [commit 10 review round 2] **User reframed the parser-boundary architecture. Option D added to the exploration doc as the new leading recommendation.** User's vision: *"I would want to query the graph for that term just straight up... it finds the edge with that exact term... no procedural transforms in the engine."* The model's first-pass framings (Option A — engine-side procedural fallthrough; Option B — boundary helper with procedural transforms) both kept normalization logic, just relocated. The user's vision is structurally cleaner: every recognized form is encoded as an edge; the engine does ONE thing — find the edge with this exact string. Catalog grows by ~150 auto-derived storage-key edges (zero hand-authoring); engine surface is pure indexed lookup. The display-label-with-spaces residual case (`'Coins From Black Hole'` from V2 clipboard) is the one escape hatch — three sub-options (D.1: declare display-label edges; D.2: one explicit `toCamelCase` retry at boundary; D.3: rely on V28 sectionized parser). Recommended D.2 as the named, scoped exception. Also reframed: the user explicitly rejected a two-method API; commit 11b ships ONE `resolveFieldByAnyKey` resolver. Decision pending in [`EXPLORATION-parser-boundary-resolution.md`](./EXPLORATION-parser-boundary-resolution.md)'s Human decision section.

- [2026-04-25] [commit 10 review round 2] **Commit 11b added to the epic as a `TODO — exploration-doc decision required first` slot.** Files-touched list explicitly references the four duplicate-normalization sites. Each source file's transitional comment now points at commit 11b by number, replacing the previous "exploration doc" generic reference. Two-way breadcrumbs verified.

- [2026-04-25] [commit 10 review round 2] **Tier handling spun off into [`EXPLORATION-tier-handling.md`](./EXPLORATION-tier-handling.md).** Three options surfaced: T1 (self-deriver), T2 (dedicated `'tier'` data type — recommended), T3 (parser-level fix in `parseShorthandNumber`). The user's leaning during discussion: *"maybe I'm going to add a specific data type for tiers."* — quoted in the doc's Human decision section. Outcome feeds commit 9's expanded scope. Both `data-parser.ts` and `run-type-detection.ts` already carry transitional comments pointing at commit 9.

- [2026-04-25] [commit 10 review round 2] **Architecture-enforcer agent concept captured in [`EXPLORATION-architecture-enforcer-agent.md`](./EXPLORATION-architecture-enforcer-agent.md).** Process / tooling concern, not a code commit. Frames the question as: the field-graph rule set has grown to ~10+ specific rules across `field-graph-for-ai.md`, `prompt-skeleton.md`, two `PATTERN.md` files, and Notes-and-findings — and the existing review agents are tuned for general patterns, not epic-specific ones. The proposed agent runs late in the per-commit review chain, audits the diff against the codified rule set, and surfaces trajectory smells (cumulative API growth, dead code, two-way breadcrumb compliance) that single-commit agents miss. Lifespan-bounded: retires or generalizes when the migration epic completes (commit 16). Decision pending.

- [2026-04-25] [commit 10 review round 2] **Stale code comment in [`field-graph/index.ts`](../../src/shared/domain/field-graph/index.ts) removed.** The "Rename-history queries (`renamesOf`, `legacyKeysOf`) are NOT exported here" comment referenced functions that had just been deleted from the barrel — an artifact of the change, not future-useful guidance. User: *"is this useful code comment for future changes? ... To me, no."* Removed. Lesson: when you remove an export, also remove any comment that explains why it WASN'T exported — the comment makes sense at the moment of removal but becomes noise immediately after.

- [2026-05-03] [commit 10 review round 3] **Tier handling decision LOCKED — Option T2 (dedicated `'tier'` data type).** User's framing during this round added a new rationale to the doc: *"a number data type would imply that you can do arithmetic on it. ... we're not going to be adding tiers together."* The arithmetic-vs-non-arithmetic distinction sharpened the case beyond the original draft's "semantic kind" framing. T2 lands in commit 9 alongside the `_runType IS_DERIVED_FROM battleReport_tier` deriver (which is a separate concern but same commit per the spec). User flagged the `+`-suffix handling as sunset-eligible: V28 exports never produce it; only V2 imports do. Eventually deletable but kept for now — `EXPLORATION-tier-handling.md` Human decision section captures the revisit trigger.

- [2026-05-03] [commit 10 review round 3] **Option D in the parser-boundary exploration was directionally accepted, but the concrete shape needed deeper analysis.** User's review surfaced five sub-questions the original three-option frame couldn't handle: (1) edge-type-aware vs edge-type-agnostic resolver — *"why can't you just look up the key and see which edges are returned?"*; (2) multi-edge collision semantics; (3) **edge categories as a first-class property** — *"is there like parsing edges and are there reading edges?"* — inbound (parsing) vs processing (in-memory queries) vs outbound (UI / export); (4) display labels — why the `toCamelCase` step at all? Why not declare `'Coins From Black Hole'` as a verbatim edge target?; (5) schema-version vs game-version distinction. Spun up [`EXPLORATION-option-d-deep-dive.md`](./EXPLORATION-option-d-deep-dive.md) as a successor doc; the parser-boundary doc now defers concrete shape there. Commit 11b's scope is locked but its implementation shape depends on the deep-dive's Human decision.

- [2026-05-03] [commit 10 review round 3] **Two tooling commits added to the epic — Phase 3.6.** Commit 17 (field-graph-enforcer agent per `EXPLORATION-architecture-enforcer-agent.md`) and commit 18 (exploration-doc-generator agent — captures the structural template this epic has been applying ad-hoc to every exploration). User explicitly wanted both tracked as epic commits, not as out-of-band tooling. Lifespan: enforcer is bounded (retires or generalizes at commit 16); exploration-doc-generator outlives the epic and remains a general-purpose agent.

- [2026-05-03] [commit 10 review round 3] **Pattern observation — exploration discipline grew organically, codifying it as a commit.** The migration epic has produced ~7 exploration docs so far. Each follows roughly the same structure (front-matter, recommendation summary, Human decision section, options with pros/cons, comparison matrix, per-commit impact, open questions). The user's framing: *"we have this repeated concept. I want to maybe just create a dedicated agent for just, hey, next time I want to have an exploration like this."* Codified as commit 18. Lesson for future epics: when a structural template is being applied ≥3 times, make it an agent; the AI's context window stops carrying the template recall and starts carrying substantive exploration. Same lesson would apply to the field-graph rule set — codified as commit 17.

- [2026-05-03] [commit 10 review round 3] **Process meta-observation: rabbit-hole rate is the leading indicator of architectural fitness, not progress velocity.** User: *"as we do with each of these commits, we open up more and more rabbit holes, which helps us understand the problem better. ... initially I thought phase two was just implement the edges. But as we're implementing edges, we're seeing where there are gaps in the original architecture."* This is exactly the right shape for an epic that's restructuring foundational metadata — the rabbit-hole rate slows AS the architecture stabilizes. Worth flagging in commit 16's TypeScript-vs-graph audit: count rabbit-holes-per-commit across the epic to see if the architecture stabilized or kept generating new questions through the end.

- [2026-05-03] [commit 10 review round 4 — closeout] **Option D deep-dive resolved.** User reviewed the deep-dive doc and locked all five sub-questions in the simplest mechanically-consistent shape on each axis: D-α resolver, no categories, explicit form-as-edge declarations (no `toCamelCase` transform), build-time collision errors, app-schema axis only. User reasoning: *"having lookup by the, you know, Kanako ID, legacy ID, etc. That makes, maybe it's a decent draw, trade off right now. Like we only have two, maybe three, maybe four edges that we can look up. So it's not quite a category of issue right now."* And: *"every field would have to be defined in the edge configurations... it does bring clarity, I think... so let's not do two camel casing."* The locked shape feeds commit 11b directly. Commit 10 flipped to DONE.

- [2026-05-03] [commit 10 review round 4 — closeout] **User exhaustion is a real signal — surface it.** Conversation crossed ~2 hours per commit at this point and the user explicitly asked to dial back the rabbit-holing: *"I want to maybe push on more progress and maybe create a list of commits of like, hey, here's a thing we, you know, deferred decisions, and maybe we pick them up and we dive into them, or maybe we just drop them because they're not a big deal."* Lesson for future epic work: when conversation reaches this point, the right move isn't more depth — it's a terse status / deferred-grab-bag / next-step summary. Captured as a `deferred-grab-bag.md` (proposed) or as inline "Future revisit triggers" sections in the relevant exploration docs (chosen here — see status-audit references in `prompt-skeleton.md`).

- [2026-05-03] [commit 10 review round 4 — closeout] **Deferred grab-bag captured per exploration doc, not as a separate file.** Open architectural concerns that aren't currently blocking progress: (a) edge categories, (b) auto-derivation of edges from canonical-id pattern, (c) game-version axis on edges, (d) `+`-suffix sunset for tier, (e) `resolveFieldByAnyKey` naming (folded into commit 11b's refactor). Each lives in the "Future revisit triggers" section of the relevant exploration doc rather than a standalone tracking file — keeps the context with the original decision rationale.

- [2026-05-03] [commit 10 review round 4 — closeout] **Naming nit captured for commit 11b.** User: *"resolve field by any key. I don't know, that feels like a little bit verbose for, like, just resolve field or get field or get field by key or something like that. ... The any key part is, to me, leaking, leaking implementation details."* Folded into commit 11b's scope as a naming-cleanup item (rename the function + internal index variable names; don't invent new terms like "canonical ID" / "legacy ID" — use edge-type names directly). Touches ~13 files. Done as part of 11b's refactor, not a standalone rename commit.

- [2026-05-12] [commit 6] **Section model decision: storage-prefix-aligned, not UI-grouping-derived.** The 16 declared `section:*` nodes mirror V3 storage prefixes. Each game field declares one BELONGS_TO_SECTION edge to its prefix-derived section. The UI's pre-commit-6 sub-groupings (BATTLE_REPORT_ESSENTIAL vs MISCELLANEOUS, DAMAGE_TAKEN_CONFIG mixing 3 sections, OTHER_EARNINGS_CONFIG mixing 3 sections) are gone — run-details now renders all 16 game-export sections directly, grouped by 4 categories (general / records / combat / economic). User accepted the regression: *"I'm fine with getting rid of my sections in from my, in my UI. ... I'm fine with starting with the sections tied to game export sections."* The "Modules" UI category is dropped (all module/shard fields live in `section:currencies` which renders under `economic`); `category:battleReport` was renamed to `category:general` (now contains battleReport + counts + utility). The §15.2 RENDERS_AS_IN_SECTION example (cellsEarned-as-'Cells' override) doesn't appear in current code; deferred until a real consumer needs it.

- [2026-05-12] [commit 6] **BELONGS_TO_CATEGORY folded in alongside BELONGS_TO_SECTION.** The epic listed only BELONGS_TO_SECTION + RENDERS_AS_IN_SECTION for commit 6, but the UI restructure (categories-of-sections rendering) requires section→category mapping to be graph-queryable. Per the "epic is evolving" principle, commit 6's scope expanded to include BELONGS_TO_CATEGORY edge declarations + `sectionsInCategory()` / `categoryOfSection()` / `categoriesInDisplayOrder()` queries. Edge type was already declared in commit 1's `EDGE_META` (sourceKind: 'Section', targetKind: 'Category', cardinality: 'one'). No new edge types needed.

- [2026-05-12] [commit 6] **Internal fields have no BELONGS_TO_SECTION.** `_date`, `_time`, `_notes`, `_runType`, `_rank` are app-managed metadata, not game-export data. They're excluded from BELONGS_TO_SECTION declarations and from run-details section rendering (handled at the UI layer via `isInternalField()`). The `sections.invariants.test.ts` asserts both directions: every game field has at least one section AND every internal field has zero sections. If future internal fields need a section (e.g. for form rendering), introduce a separate `section:internal` node or use APPEARS_IN_VIEW in commit 12.

- [2026-05-12] [commit 6] **Transitional `derive-display-name.ts` survives until commit 7.** Plain section rendering needs per-field display names. Pre-commit-6 those names lived in section-config.ts's PlainFieldsConfig entries. The graph doesn't carry HAS_DISPLAY_NAME edges for game fields yet — that's commit 7's territory (per the comment in section-config.ts before deletion). For now, a `deriveDisplayName(fieldId)` helper Title-Cases the camelCase suffix (`coins_goldenTower` → `Golden Tower`, `counts_hitsAbsorbedByEnergyShield` → `Hits Absorbed By Energy Shield`). Most fields render correctly; a few curated names regress (`Guardian Medals` → `Medals`, `Cash` → `Cash Earned`, `Golden Tower (Cash)` → `Golden Tower`). User accepted the regression. Commit 7 deletes `derive-display-name.ts` once HAS_DISPLAY_NAME edges land.

- [2026-05-12] [commit 6] **`section-config.ts` survives transitionally as a breakdown-only registry.** The plain-field configs (BATTLE_REPORT_ESSENTIAL, DAMAGE_TAKEN_CONFIG, etc.) are deleted; the `BreakdownConfig` definitions for damage/coins/enemies remain, keyed by section id (`SECTION_BREAKDOWNS['section:damage'] = DAMAGE_DEALT`). The breakdowns carry per-source colors that should become HAS_COLOR edges in commit 7's IS_SOURCE_OF cutover. Until then, breakdowns dispatch via the registry. Also retained: `HIDDEN_FROM_RUN_DETAILS` (single entry: `battleReport_battleDate`, already shown in run card header). Pre-commit-6 the file had ~340 lines; post-commit it's ~120 lines.

- [2026-05-12] [commit 6] **Per-category section components collapsed into one generic `CategorySection`.** Pre-commit `BattleReportSection`, `CombatSection`, `EconomicSection`, `ModulesSection` were each hardcoded to render specific category data (`data.battleReport.essential`, `data.combat.damageDealt`, etc.). Post-commit, a single `CategorySection` renders any category by iterating its sections and dispatching by `kind: 'plain' | 'breakdown'`. Run-details main file loops `data.categories.map(c => <CategorySection data={c} />)`. The category layout (single column vs two-column grid) is unified — all categories now use a 2-column responsive grid for sections. This is a small UX change the user accepted as part of the restructure.

- [2026-05-12] [commit 6] **File-split convention for large edge files.** The BELONGS_TO_SECTION declarations alone (142 edges + ~140 field-node imports) exceeded the project's max-lines=300 limit when combined with BELONGS_TO_CATEGORY in one `sections.edges.ts`. Split the concept into `sections.belongs-to-section.edges.ts` (per-section memberships, ~300 lines) and `sections.edges.ts` (16 BELONGS_TO_CATEGORY edges + the rollup), following the `* as f` namespace-import pattern established by `renames.edges.ts`. Same per-concept folder; multiple `.edges.ts` files concatenated via the folder's primary `*.edges.ts` rollup. `edges/PATTERN.md` already implies this is allowed ("not every concept needs every file"), but a future PATTERN.md edit could codify the split-when-too-large pattern explicitly.

- [2026-05-12] [commit 6] **Trajectory: engine API surface unchanged.** Per commit 5b's locked design, no methods added to `FieldGraph`. Three new queries (`sectionsInCategory`, `categoryOfSection`, `categoriesInDisplayOrder`) live in `catalog/edges/sections/sections.queries.ts`; the singleton-bound wrappers in the top barrel grow by three. `FieldGraph` stays at 8 primitive methods.

- [2026-05-12] [commit 6] **Conditional-logic check: passes, with one carve-out.** The pre-commit `use-run-details-data.ts` hardcoded the data shape: `{ battleReport: {...}, combat: {...}, economic: {...}, modules: {...} }` with specific field-list arrays per UI sub-section. Post-commit the shape is generic `{ categories: CategoryData[], uncategorized }` derived from `categoriesInDisplayOrder() → sectionsInCategory() → fieldsInSection()`. Zero per-section / per-category branching in the hook. The one remaining if/else: `SECTION_BREAKDOWNS[sectionId]` lookup to decide plain vs breakdown rendering — that's owned by commit 7's IS_SOURCE_OF cutover, which will replace the registry with `sourcesOf(totalField)` queries.

- [2026-05-12] [commit 6] **Dead-code drained: `findUncategorizedFields` helper inlined.** The pre-commit `findUncategorizedFields(run, categorizedFields, skipFields)` took explicit Set parameters because the categorized list was hand-authored. Post-commit, the uncategorized logic is inlined in `use-run-details-data.ts:buildUncategorized` and derives the categorized set from graph queries (every field in every section + every breakdown source). One fewer function to maintain.

- [2026-05-12] [commit 6] **The "synthetic cellsPerHour fallback" feature was dropped.** Pre-commit, `use-run-details-data` added a derived `cellsPerHour` item to other earnings when `battleReport_cellsPerHour` was absent (V2-compatibility for older runs). Post-commit, V28 exports always carry `battleReport_cellsPerHour` as a real field in `section:battleReport`, and V2 runs migrate through RENAMED_FROM edges. The synthetic fallback was specific to V2-era render-time math; dropping it simplifies the data hook by ~10 lines. No UX regression for current-version users.

- [2026-05-12] [commit 6] **E2E flakiness observed but unrelated.** The bulk-import seed test occasionally times out at `waitForPageLoad` (10s) when Vite's cold-start takes >25s. Reproducible only on a cold dev server; warm reruns pass consistently. Not introduced by this commit (graph build is fast — `npx vitest run` builds the full catalog with 142 BELONGS_TO_SECTION edges + 16 BELONGS_TO_CATEGORY edges in <1s). The flake is Vite-cold-start related; commit 14 (commit-16-style tooling pass) could bump the page-load timeout or add a `waitForServer` heuristic if the rate becomes painful.

- [2026-05-12] [commit 6] **Material question surfaced + resolved before implementation.** Pre-implementation question: are `section:*` nodes storage-prefix-aligned or UI-grouping-aligned? Architecture spec §15 + `catalog/PATTERN.md` describe BELONGS_TO_SECTION as UI grouping ("Q3 behavior? yes — UI grouping"), but the existing 16 section nodes were named after V3 storage prefixes. User decision: storage-prefix sections; UI restructures to mirror them; cross-cutting UI groupings (ESSENTIAL/MISC, mixed DAMAGE_TAKEN) are sacrificed. This is the simplest mechanically-consistent shape — sections = storage prefixes; categories = UI rollups; multi-section membership is reserved for future cross-cutting needs (e.g. `battleReport_cellsEarned` belonging to currencies as a future commit-7 polish). Pattern: when a material question surfaces, AskUserQuestion is cheap; rabbit-holing into alternatives that the user can decide in 60 seconds is expensive.

- [2026-05-12] [commit 7] **Two distinct field-to-total edge types: `IS_SOURCE_OF` (sum-to-total) and `IS_MEASURED_AGAINST` (reference yardstick).** Three breakdowns model cleanly as `IS_SOURCE_OF` — sources literally sum to the total: coins → `battleReport_coinsEarned`, damage → `damage_damageDealt`, enemy types → `totalEnemies_totalEnemies`. Three other run-details breakdowns (enemiesHitBy, enemiesDestroyedBy, killedWithEffectActive) share `totalEnemies_totalEnemies` as denominator but their fields don't sum to it (a single enemy can be hit by orbs AND thorns AND chain lightning). First-pass design parked these under a future `APPEARS_IN_VIEW` (commit 12), but the user pushed back: the relationship is field-to-field and structural (it exists independent of any UI view), so it warrants its own edge type. Decision: `IS_MEASURED_AGAINST` — captures the directionality ("source field is primary; target is the reference yardstick"), reads naturally in prose, doesn't claim partition or sum. Cardinality `'many'` (allows multiple anchors in principle; today each field has one). Edges live in `catalog/edges/sources/sources.measured-against.edges.ts` (split from `sources.edges.ts` since both files together exceed comfortable single-file size). New queries: `fieldsMeasuredAgainst(total)` and `measurementTargetsOf(field)`.

- [2026-05-12] [commit 7] **`section-config.ts` reduced to per-section denominator hints.** Pre-commit: ~340-line file with hand-authored source lists, display names, and colors duplicated across 6 breakdown configs. Post-commit: ~60 lines declaring 6 entries with `{ totalField, perHourField? }`. No more `explicitSources`, no `label`, no `skipDiscrepancy`. The consumer (`use-run-details-data.ts`) detects genuine-vs-supplementary structurally: "does any field in this section have IS_SOURCE_OF the denominator? → genuine (return all `sourcesOf` including cross-section) ; else supplementary (return `fieldsInSection ∩ fieldsMeasuredAgainst`)." `skipDiscrepancy` is auto-set to `isSupplementary`. The structural detection means adding/removing a section's breakdown shape is now a graph edit, not a config edit.

- [2026-05-12] [commit 7] **`derive-display-name.ts` fully retired — HAS_DISPLAY_NAME universal across Field, Section, and Category.** `EDGE_META.HAS_DISPLAY_NAME.sourceKind` broadened from `['Field', 'EnumValue']` to `['Field', 'EnumValue', 'Section', 'Category']` (one-line meta change). `presentation/presentation.edges.ts` now declares HAS_DISPLAY_NAME for: every Field (~150 — explicit override via a curated map for the 77 breakdown sources + ~17 fields whose curated name diverges from the Title-Cased camelCase default; the rest get the default via a catalog-time helper), every Section (16), every Category (4). Total: ~170 HAS_DISPLAY_NAME edges + 77 HAS_COLOR edges. Invariant test asserts every Field, Section, and Category has the edge — `displayNameOf()` is total over those kinds. The runtime fallback (`displayNameOf(id) ?? deriveDisplayName(id)`) is gone from every consumer; `displayNameOf(id) ?? id` is the new shape, and the `?? id` path is dead per the invariant. `derive-display-name.ts` deleted entirely.

- [2026-05-12] [commit 7] **`HAS_DISPLAY_NAME` / `HAS_COLOR` for Field nodes landed in `catalog/edges/presentation/`, not under `sources/`.** Per `edges/PATTERN.md`'s cross-source-kind guidance, presentation edge types span multiple source kinds (Field, EnumValue, Section, Category as of this commit). Enum-value presentation stays in `enum-values/` (tightly coupled to the value declarations); everything else lives in `presentation/`. New invariants: `presentation.invariants.test.ts` asserts universal HAS_DISPLAY_NAME coverage for Field/Section/Category and HAS_COLOR coverage for breakdown source fields. `sources.invariants.test.ts` covers IS_SOURCE_OF shape against the `supportedFields.json` catalog.

- [2026-05-12] [commit 7] **Discrepancy domain moved to `src/shared/domain/source-discrepancy/`.** The pre-commit `breakdown-sources/` directory mixed two concerns: field configs (deleted) and discrepancy detection (consumed by both run-details and source-analysis). Per the EPIC's "delete breakdown-sources/index.ts" mandate, splitting was cleaner than leaving a slim directory. Three consumer files (`breakdown-calculations.ts`, `breakdown-tooltip.ts`, `period-grouping.ts`) updated their import path; no behavior changes. The `breakdown-sources/` directory and all its files deleted entirely.

- [2026-05-12] [commit 7] **`FIELD_ALIASES` in source-analysis deleted.** Pre-commit, `source-extraction.ts` checked aliases (e.g. `coinsFromBlackhole` → `coins_blackHole`) when the canonical key wasn't present. Post-commit-10 (`RENAMED_FROM` cutover), legacy spellings are canonicalized at the parser boundary; by the time runs reach aggregators, all field keys are V3 canonical. The alias map was already empty in V3 — deletion just removes dead code. Pre-existing test `source-extraction.test.ts:70` asserts the "legacy variant returns 0" behavior and still passes.

- [2026-05-12] [commit 7] **Conditional-logic check passes.** The `SECTION_BREAKDOWNS[sectionId]` lookup remains as a 6-entry registry-table dispatch — it never was a ladder. Display names, colors, and genuine-breakdown source lists all come from the graph; the registry only carries UI hints the graph doesn't yet express (denominator field, perHourField, skipDiscrepancy flag, transitional `explicitSources`). No `if (fieldName === ...)` or `switch (sectionId)` ladders survive in either run-details or source-analysis. Commit 6's deferred carve-out ("the registry needs commit 7's IS_SOURCE_OF cutover") is resolved for genuine breakdowns and scoped into commit 12 for supplementary breakdowns.

- [2026-05-12] [commit 7] **Trajectory: engine API surface unchanged (still 8 primitives).** All queries used by this commit (`sourcesOf`, `displayNameOf`, `colorOf`) were declared in commit 5b as singleton-bound wrappers; the underlying per-concept query functions already existed. Zero methods added to `FieldGraph`. Zero new singleton-bound wrappers added to the top barrel. The 5b restructure continues to pay off — broadening HAS_DISPLAY_NAME source-kind coverage and adding ~170 edges didn't require touching the engine class or the barrel index.

- [2026-05-12] [commit 7] **First multi-section field: `totalEnemies_summonedEnemies` belongs to both `section:totalEnemies` (V3 storage prefix) and `section:killedWithEffectActive` (rendered as part of that supplementary breakdown).** The architecture allows multi-section membership (commit 6 notes flagged it as "reserved for future cross-cutting needs"); this commit's UI parity requirement made it the first real case. Without multi-section, the killedWithEffectActive breakdown would have lost summonedEnemies — a UX regression we explicitly avoided. The `sections.invariants.test.ts` "every game field has at least one section" rule already permits >1; no test change needed.

- [2026-05-12] [commit 7] **Consumer logic detects genuine vs supplementary structurally — no flag in `section-config.ts`.** Earlier draft used an `explicitSources` array as the signal; user pushback reframed it as a structural edge-type concern. New shape:
  ```ts
  const isGenuine = sourcesOf(totalField).some(id => fieldsInSection(sectionId).has(id))
  ```
  Reads cleanly: "this section is the genuine breakdown of `totalField` if it owns one of its sources." Supplementary-by-construction otherwise. Side benefit: the cross-section source case (`healthRegenerated_lifesteal` → `damage_damageDealt`) is preserved automatically — genuine breakdowns return ALL `sourcesOf` results, including sources in sibling sections.

- [2026-05-12] [commit 7] **Commit 12 scope reverted: supplementary breakdowns no longer feed APPEARS_IN_VIEW.** The earlier expansion of commit 12 to "absorb the supplementary breakdowns in section-config.ts" became unnecessary once `IS_MEASURED_AGAINST` landed here. Commit 12 stays focused on its original concern: which fields appear in chart / analysis / filter views. The inherited open questions from commits 2 and 6 remain in commit 12's entry.

- [2026-05-12] [commit 7] **Three concept folders, one edge type per folder where the type stands alone.** Initial pass commingled `IS_SOURCE_OF` and `IS_MEASURED_AGAINST` under `sources/`. User pushed back: two structurally different edge types shouldn't share a concept folder just because both relate to "field-to-total." Split into `catalog/edges/sources/` (IS_SOURCE_OF), `catalog/edges/measurements/` (IS_MEASURED_AGAINST), and `catalog/edges/breakdowns/` (Section-level rendering metadata: HAS_BREAKDOWN_TOTAL, HAS_BREAKDOWN_RATE). Each folder owns its `.edges.ts` + `.queries.ts` + `.queries.test.ts` + `.invariants.test.ts`. Cleaner separation; one edge type per file in the typical case; `PATTERN.md`'s "concepts may have 2-3 closely-related types" exception narrows to genuine overlap (e.g. `enum-values/` still pairs ACCEPTS_VALUE + HAS_STRING_VALUE because those derive from the same TS source-of-truth tuple).

- [2026-05-12] [commit 7] **`section-config.ts` deleted entirely — every per-section breakdown fact lives in the graph now.** Two new Section-source edges (`Section → Field`):
  - `HAS_BREAKDOWN_TOTAL` (cardinality `'one'`) — "this section renders as a breakdown of this denominator field."
  - `HAS_BREAKDOWN_RATE` (cardinality `'one'`) — "this section's breakdown displays this per-hour rate alongside the total." Optional companion.

  Six `HAS_BREAKDOWN_TOTAL` declarations + one `HAS_BREAKDOWN_RATE` (coins → coinsPerHour). The consumer queries `breakdownTotalOf(sectionId)` to decide plain-vs-breakdown rendering; `breakdownRateOf(sectionId)` for the rate companion. Adding a new breakdown section is now one edge declaration. The lone leftover from the old file — `HIDDEN_FROM_RUN_DETAILS = Set(['battleReport_battleDate'])` — inlined into the consumer with a one-line comment pointing at commit 12 (it's truly a per-view APPEARS_IN_VIEW exclusion, not breakdown metadata).

- [2026-05-12] [commit 7] **First Section-source edges in the catalog.** Pre-commit, Section nodes were only edge targets (BELONGS_TO_SECTION from Fields, BELONGS_TO_CATEGORY from Sections-to-Categories). With HAS_BREAKDOWN_TOTAL and HAS_BREAKDOWN_RATE, Sections also become edge sources. Future Section-source edges (per-section UI hints, rendering rules) follow the same shape.

- [2026-05-12] [commit 7] **Companion invariant: every breakdown section has at least one renderable source field.** New `breakdowns.invariants.test.ts` asserts that for every section X with `HAS_BREAKDOWN_TOTAL Y`, either some field in X is `IS_SOURCE_OF Y` (genuine) or some field in X is `IS_MEASURED_AGAINST Y` (supplementary). Catches the "breakdown section that points at a total but has no fields to render" gap structurally.

- [2026-05-12] [commit 7] **No suppressions added.** Zero new `eslint-disable`, `test.skip`, `@ts-expect-error`, or config overrides. Migration-era ledger unchanged.
